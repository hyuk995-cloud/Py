# def read_congif(filename):
# """
# 파일형식:
#     key=value(각 줄)
#     #시작은 주석
# 반환:  key value 형태 딕셔너리
# """

# config {}
# try:
#     with open(filenanem,"r", encdoing="utf-8") as file:
#         for line_in file: #한줄씩 읽기
#             line = line.strip() #양공백제거
#             if not line or line.startswith
#             continue
#         if "=" in line key, value = line.split(="1")#첫번째 기준으로 나누기
# c   onfig[key.strip() = value.strip]

# return config
# except FileNotFoundError:
#     print(f"{filename}을 찾을 수 없습니다.")
